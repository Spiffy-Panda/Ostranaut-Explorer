{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonExplosion]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "fDmgRadiusMax": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fDmgRadiusMin": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fDmgRayMax": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fDmgRayMin": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fRadius": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fRadiusRay": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fRadiusStat": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fStatMax": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fStatMin": {
                "type": "number",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nRaysMax": {
                "type": "integer",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nRaysMin": {
                "type": "integer",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strAMode": {
                "type": "string",
                "description": "Refers to entry within attackmodes.json."
            },
            "strAudioEmitter": {
                "type": "string",
                "description": "Refers to entry within audioemitters.json."
            },
            "strStat": {
                "type": "string",
                "description": "(Per JsonExplosion decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
