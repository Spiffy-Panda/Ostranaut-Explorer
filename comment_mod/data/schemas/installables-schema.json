{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonInstallable]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "CTAllowUs": {
                "type": "string",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "CTThem": {
                "type": "string",
                "description": "Refers to entry within condtrigs.json."
            },
            "CTUs": {
                "type": "string",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "aInputs": {
                "type": "array",
                "description": "Each entry is a condition string (Name=value*duration) — the name refers to entry within condtrigs.json."
            },
            "aInverse": {
                "type": "array",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "aLootCOs": {
                "type": "array",
                "description": "Array of names — each refers to entry within loot.json (also resolves into condowners, items).",
                "x-targets": [
                    "loot",
                    "condowners",
                    "items"
                ]
            },
            "aToolCTsUse": {
                "type": "array",
                "description": "Array of names — each refers to entry within condtrigs.json."
            },
            "bHeadless": {
                "type": "boolean",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bNoDestructable": {
                "type": "boolean",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bNoJobMenu": {
                "type": "boolean",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fDuration": {
                "type": "number",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fTargetPointRange": {
                "type": "number",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strActionCO": {
                "type": "string",
                "description": "Refers to entry within loot.json (also resolves into condowners, items).",
                "x-targets": [
                    "loot",
                    "condowners",
                    "items"
                ]
            },
            "strActionGroup": {
                "type": "string",
                "description": "Enum tag (free-form group name). NOT a strName reference."
            },
            "strAllowLootCTsThem": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strAllowLootCTsUs": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strBuildType": {
                "type": "string",
                "description": "Enum tag (free-form group name). NOT a strName reference."
            },
            "strCTThemMultCondTools": {
                "type": "string",
                "description": "Refers to entry within conditions_simple.json."
            },
            "strCTThemMultCondUs": {
                "type": "string",
                "description": "Refers to entry within conditions_simple.json."
            },
            "strFinishInteraction": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strInteractionName": {
                "type": "string",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strInteractionTemplate": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strJobType": {
                "type": "string",
                "description": "Enum tag (free-form group name). NOT a strName reference."
            },
            "strLootOut": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strPersistentCO": {
                "type": "string",
                "description": "(Per JsonInstallable decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strProgressStat": {
                "type": "string",
                "description": "Refers to entry within conditions_simple.json."
            },
            "strStartInstall": {
                "type": "string",
                "description": "Refers to entry within loot.json (also resolves into condowners).",
                "x-targets": [
                    "loot",
                    "condowners"
                ]
            }
        }
    }
}
