{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonJobItems]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aCTsDeliver": {
                "type": "array",
                "description": "Each entry is a condition string (Name=value*duration) — the name refers to entry within condtrigs.json."
            },
            "strFriendlyName": {
                "type": "string",
                "description": "(Per JsonJobItems decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strLootPickup": {
                "type": "string",
                "description": "Refers to entry within loot.json (also resolves into condowners, items).",
                "x-targets": [
                    "loot",
                    "condowners",
                    "items"
                ]
            }
        }
    }
}
