{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonCOOverlay]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aDestSwaps": {
                "type": "array",
                "description": "Array of names — each refers to entry within conditions.json (also resolves into loot).",
                "x-targets": [
                    "conditions",
                    "loot"
                ]
            },
            "aInteractionsReplace": {
                "type": "array",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "mapAltItemDefs": {
                "type": "array",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "mapAltSlotImgs": {
                "type": "array",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "mapGUIPropMaps": {
                "type": "array",
                "description": "Array of names — each refers to entry within guipropmaps.json."
            },
            "mapModeSwitches": {
                "type": "array",
                "description": "Array of names — each refers to entry within loot.json (also resolves into condowners).",
                "x-targets": [
                    "loot",
                    "condowners"
                ]
            },
            "mapSlotEffects": {
                "type": "array",
                "description": "Array of names — each refers to entry within slots.json (also resolves into slot_effects).",
                "x-targets": [
                    "slots",
                    "slot_effects"
                ]
            },
            "strCOBase": {
                "type": "string",
                "description": "Refers to entry within condowners.json (also resolves into items, loot).",
                "x-targets": [
                    "condowners",
                    "items",
                    "loot"
                ]
            },
            "strCondLoot": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strDesc": {
                "type": "string",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strDmgColor": {
                "type": "string",
                "description": "Refers to entry within colors.json."
            },
            "strImg": {
                "type": "string",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strImgDamaged": {
                "type": "string",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strImgNorm": {
                "type": "string",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNameFriendly": {
                "type": "string",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNameShort": {
                "type": "string",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strPortraitImg": {
                "type": "string",
                "description": "(Per JsonCOOverlay decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
