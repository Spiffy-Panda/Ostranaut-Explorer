{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonRacingLeague, JsonRaceTrack]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aTracks": {
                "type": "array",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "ctEntryRequirements": {
                "type": "string",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "ctLeagueHost": {
                "type": "string",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fAvgLapTime": {
                "type": "number",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fOrbitHeightKM": {
                "type": "number",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nLaps": {
                "type": "integer",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strDescription": {
                "type": "string",
                "description": "Player-facing description text."
            },
            "strEntryFeeLedgerDef": {
                "type": "string",
                "description": "Refers to entry within ledgerdefs.json."
            },
            "strImgPath": {
                "type": "string",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNameFriendly": {
                "type": "string",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strPsPecParticipant": {
                "type": "string",
                "description": "Refers to entry within personspecs.json."
            },
            "strStartingLoot": {
                "type": "string",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strTrackType": {
                "type": "string",
                "description": "(Per JsonRacingLeague/JsonRaceTrack decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
