{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonCareer]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aEvents": {
                "type": "array",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "aEventsMoney": {
                "type": "array",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "aEventsShip": {
                "type": "array",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "aSkillsFirst": {
                "type": "array",
                "description": "Array of names — each refers to entry within conditions_simple.json."
            },
            "aSkillsHobby": {
                "type": "array",
                "description": "Array of names — each refers to entry within conditions_simple.json."
            },
            "aSkillsNext": {
                "type": "array",
                "description": "Array of names — each refers to entry within conditions_simple.json."
            },
            "bComingSoon": {
                "type": "boolean",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bHide": {
                "type": "boolean",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nFirst": {
                "type": "integer",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nHobby": {
                "type": "integer",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nNext": {
                "type": "integer",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strCTPrereqs": {
                "type": "string",
                "description": "Refers to entry within condtrigs.json."
            },
            "strLootConds": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strLootCondsNext": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strNameFriendly": {
                "type": "string",
                "description": "(Per JsonCareer decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
