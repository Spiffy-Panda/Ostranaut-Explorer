{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonParallax]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aSunLights": {
                "type": "array",
                "description": "Array of names — each refers to entry within lights.json (also resolves into colors).",
                "x-targets": [
                    "lights",
                    "colors"
                ]
            },
            "fDistortion": {
                "type": "number",
                "description": "(Per JsonParallax decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fLayerScaleFactor": {
                "type": "number",
                "description": "(Per JsonParallax decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fRateX": {
                "type": "number",
                "description": "(Per JsonParallax decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fRateY": {
                "type": "number",
                "description": "(Per JsonParallax decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nLayers": {
                "type": "integer",
                "description": "(Per JsonParallax decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strLootSpriteList": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strPattern": {
                "type": "string",
                "description": "(Per JsonParallax decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
