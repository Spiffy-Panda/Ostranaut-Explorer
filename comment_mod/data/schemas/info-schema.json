{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonInfoNode]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "escapeChar": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nArticleTitleStyle": {
                "type": "integer",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nLookup": {
                "type": "integer",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nMainPanelFormat": {
                "type": "integer",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strArticleBody": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strArticleTitle": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strImage": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strLookup": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNodeLabel": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNodeParent": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strTutorialKey": {
                "type": "string",
                "description": "(Per JsonInfoNode decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
