{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonPlot]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aPhaseTitles": {
                "type": "array",
                "description": "(Per JsonPlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bNoCheck": {
                "type": "boolean",
                "description": "(Per JsonPlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strCancelBeat": {
                "type": "string",
                "description": "Refers to entry within plot_beats.json."
            },
            "strLootNextPlot": {
                "type": "string",
                "description": "(Per JsonPlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNameFriendly": {
                "type": "string",
                "description": "(Per JsonPlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
