{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonZoneTrigger]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "bQueueInteractionInsert": {
                "type": "boolean",
                "description": "(Per JsonZoneTrigger decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bRemoveOnTrigger": {
                "type": "boolean",
                "description": "(Per JsonZoneTrigger decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bRunEncounterInterrupt": {
                "type": "boolean",
                "description": "(Per JsonZoneTrigger decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strApplyInteractionChain": {
                "type": "string",
                "description": "(Per JsonZoneTrigger decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strQueueInteraction": {
                "type": "string",
                "description": "(Per JsonZoneTrigger decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strRunEncounter": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            }
        }
    }
}
