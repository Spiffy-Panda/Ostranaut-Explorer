{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonSlotEffects]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aSlotsAdded": {
                "type": "array",
                "description": "(Per JsonSlotEffects decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "aSlotsSecondary": {
                "type": "array",
                "description": "Array of names — each refers to entry within slots.json."
            },
            "bMirror": {
                "type": "boolean",
                "description": "(Per JsonSlotEffects decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bWholeBody": {
                "type": "boolean",
                "description": "(Per JsonSlotEffects decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "mapMeshTextures": {
                "type": "array",
                "description": "(Per JsonSlotEffects decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strIASlot": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strIASlotParents": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strIAUnslot": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strIAUnslotParents": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strSlotImage": {
                "type": "string",
                "description": "(Per JsonSlotEffects decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strSlotImageUnder": {
                "type": "string",
                "description": "(Per JsonSlotEffects decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strSlotPrimary": {
                "type": "string",
                "description": "(Per JsonSlotEffects decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
