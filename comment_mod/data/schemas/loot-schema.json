{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of the loot entry, as referred to in other files."
            },
            "strType": {
                "type": "string",
                "description": "Enum tag dispatching to game code (condition, item, trigger, interaction, condrule, lifeevent, ship, repeat, surviveo2, ...). NOT a strName reference; render as a badge on detail pages and use to route aCOs targets per the type-routed-refs design.",
                "x-source": "https://ostranauts.wiki.gg/wiki/Modding/Loot#strType",
                "x-enum": true
            },
            "aCOs": {
                "type": "array",
                "description": "Condition strings (Name=chance x min-max) applied by this loot entry. Target folder is type-routed by sibling strType (condition→conditions, item→condowners, trigger→condtrigs, interaction→interactions, condrule→condrules, lifeevent→lifeevents, ship→ships); the v1 fallback treats them as refs to entry within conditions.json which catches the most common case.",
                "x-source": "https://ostranauts.wiki.gg/wiki/Modding/Loot#aCOs"
            },
            "aLoots": {
                "type": "array",
                "description": "Nested loot entries; each refers to entry within loot.json"
            }
        }
    }
}
