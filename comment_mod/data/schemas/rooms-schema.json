{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonRoomSpec]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aForbids": {
                "type": "array",
                "description": "Each entry is a condition string (Name=value*duration) — the name refers to entry within condtrigs.json."
            },
            "aReqs": {
                "type": "array",
                "description": "Each entry is a condition string (Name=value*duration) — the name refers to entry within condtrigs.json."
            },
            "bAllowVoid": {
                "type": "boolean",
                "description": "(Per JsonRoomSpec decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fValueModifier": {
                "type": "number",
                "description": "(Per JsonRoomSpec decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nMaxTileSize": {
                "type": "integer",
                "description": "(Per JsonRoomSpec decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nMinTileSize": {
                "type": "integer",
                "description": "(Per JsonRoomSpec decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nPriority": {
                "type": "integer",
                "description": "(Per JsonRoomSpec decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strIconName": {
                "type": "string",
                "description": "(Per JsonRoomSpec decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNameFriendly": {
                "type": "string",
                "description": "(Per JsonRoomSpec decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
