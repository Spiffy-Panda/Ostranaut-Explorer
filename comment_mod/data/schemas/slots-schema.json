{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonSlot]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "bAlignSlot": {
                "type": "boolean",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bAllowStacks": {
                "type": "boolean",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bCarried": {
                "type": "boolean",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bHide": {
                "type": "boolean",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bHoldSlot": {
                "type": "boolean",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fAlignX": {
                "type": "number",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fAlignY": {
                "type": "number",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nDepth": {
                "type": "integer",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nItems": {
                "type": "integer",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strCTAutoSlot": {
                "type": "string",
                "description": "Refers to entry within condtrigs.json."
            },
            "strHitboxImage": {
                "type": "string",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strIconImage": {
                "type": "string",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strNameFriendly": {
                "type": "string",
                "description": "(Per JsonSlot decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
