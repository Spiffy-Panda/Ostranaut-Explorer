{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonLedgerDef]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "bPaid": {
                "type": "boolean",
                "description": "(Per JsonLedgerDef decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bPayATC": {
                "type": "boolean",
                "description": "(Per JsonLedgerDef decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fAmount": {
                "type": "number",
                "description": "(Per JsonLedgerDef decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strCurrency": {
                "type": "string",
                "description": "Refers to entry within conditions_simple.json."
            },
            "strDesc": {
                "type": "string",
                "description": "(Per JsonLedgerDef decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strFrequency": {
                "type": "string",
                "description": "(Per JsonLedgerDef decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strPSpecOrCtPayee": {
                "type": "string",
                "description": "Refers to entry within personspecs.json."
            }
        }
    }
}
