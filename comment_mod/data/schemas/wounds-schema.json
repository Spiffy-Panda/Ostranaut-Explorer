{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonWound]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aSlotOverlaps": {
                "type": "array",
                "description": "Array of names — each refers to entry within slots.json."
            },
            "bBleeds": {
                "type": "boolean",
                "description": "(Per JsonWound decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fHitChance": {
                "type": "number",
                "description": "(Per JsonWound decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strLootBluntVerbs": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strLootCutVerbs": {
                "type": "string",
                "description": "Refers to entry within loot.json."
            },
            "strPainPNG": {
                "type": "string",
                "description": "(Per JsonWound decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
