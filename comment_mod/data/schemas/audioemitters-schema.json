{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonAudioEmitter]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "fLoPassFreq": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fLoPassFreqOccluded": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fMaxDistance": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fMinDistance": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fSpatialBlend": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fSteadyDelay": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fTransDuration": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fVolumePickup": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fVolumeSteady": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fVolumeTrans": {
                "type": "number",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strClipPickup": {
                "type": "string",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strClipSteady": {
                "type": "string",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strClipTrans": {
                "type": "string",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strFalloffCurve": {
                "type": "string",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strLowPassCurve": {
                "type": "string",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strMixerName": {
                "type": "string",
                "description": "(Per JsonAudioEmitter decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
