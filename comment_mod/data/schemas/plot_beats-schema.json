{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonPlotBeat]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "bNoObjective": {
                "type": "boolean",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bNoticeable": {
                "type": "boolean",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bRelease": {
                "type": "boolean",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bTension": {
                "type": "boolean",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "nPhaseChange": {
                "type": "integer",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strIATrigger": {
                "type": "string",
                "description": "Encoded array (split on ',') — first token of each entry refers to entry within interactions.json. Hand-tune x-shape (LootItmsArray / InverseArray) before this fires."
            },
            "strTokenSet3rd": {
                "type": "string",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strTokenSetThem": {
                "type": "string",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strTokenSetUs": {
                "type": "string",
                "description": "(Per JsonPlotBeat decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            }
        }
    }
}
