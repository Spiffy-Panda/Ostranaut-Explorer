{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonPowerInfo]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aInputPts": {
                "type": "array",
                "description": "(Per JsonPowerInfo decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bAllowExtPower": {
                "type": "boolean",
                "description": "(Per JsonPowerInfo decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fAmount": {
                "type": "number",
                "description": "(Per JsonPowerInfo decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fOverrideAmount": {
                "type": "number",
                "description": "(Per JsonPowerInfo decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strIntPowerOff": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strIntPowerOn": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            },
            "strOverrideCond": {
                "type": "string",
                "description": "(Per JsonPowerInfo decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strPowerSourceCT": {
                "type": "string",
                "description": "(Per JsonPowerInfo decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strRechargeCT": {
                "type": "string",
                "description": "(Per JsonPowerInfo decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strUsePowerCT": {
                "type": "string",
                "description": "Refers to entry within condtrigs.json."
            }
        }
    }
}
