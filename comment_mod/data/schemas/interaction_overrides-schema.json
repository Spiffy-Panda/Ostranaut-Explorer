{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonInteractionOverride]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "aOverrideValues": {
                "type": "array",
                "description": "(Per JsonInteractionOverride decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strFind": {
                "type": "string",
                "description": "Refers to entry within plots.json."
            },
            "strReplace": {
                "type": "string",
                "description": "(Per JsonInteractionOverride decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strTemplateIA": {
                "type": "string",
                "description": "Refers to entry within interactions.json."
            }
        }
    }
}
