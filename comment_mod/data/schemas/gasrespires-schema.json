{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Generated by utils/python/generate_missing_schemas.py from decomp DTO union [JsonGasRespire]; descriptions on ref-carrier fields use the candidate detector's top target. Hand-tune fields flagged 'no auto-detected ref signal' if they are strName references.",
    "type": "array",
    "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
            "strName": {
                "type": "string",
                "description": "The code name of this entry, as referred to in other files."
            },
            "bAllowExternA": {
                "type": "boolean",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "bAllowExternB": {
                "type": "boolean",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fSignalCheckRate": {
                "type": "number",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "fVol": {
                "type": "number",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strAudioEmitterAir": {
                "type": "string",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strAudioEmitterPump": {
                "type": "string",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strCTA": {
                "type": "string",
                "description": "Refers to entry within condtrigs.json."
            },
            "strCTB": {
                "type": "string",
                "description": "Refers to entry within condtrigs.json."
            },
            "strPtA": {
                "type": "string",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strPtB": {
                "type": "string",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strSignalCTA": {
                "type": "string",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strSignalCTB": {
                "type": "string",
                "description": "(Per JsonGasRespire decomp; no auto-detected ref signal — hand-tune description if this field is a strName reference.)"
            },
            "strSignalCTMain": {
                "type": "string",
                "description": "Refers to entry within condtrigs.json."
            }
        }
    }
}
